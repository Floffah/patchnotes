import React, { FC } from "react";
import { NextSeo } from "next-seo";

const Index: FC = (_p) => {
    return (
        <>
            <NextSeo title="Home" />
        </>
    );
};

export default Index;
